# Django and WebSockets
Django==4.2.5
daphne==4.0.0
channels==4.0.0

# OpenCV (Ensure compatibility with ONNX)
opencv-python-headless==4.5.5.64

# Machine Learning & ONNX Support
torch==2.0.1
torchvision==0.15.2
onnx==1.12.0
onnxruntime==1.14.1
numpy==1.24.3  # Compatible with OpenCV and Torch

# WebSockets & Async Features
asgiref==3.7.2

# Other Dependencies
pillow==9.5.0
requests==2.31.0
whitenoise==6.5.0  # For serving static files
gunicorn==20.1.0  # Needed for Render deployment

# Database (If using PostgreSQL, install psycopg2)
psycopg2-binary==2.9.6  # Only needed for PostgreSQL

# Environment Variables
python-dotenv==1.0.0
